"""二手烟暴露与疾病负担：可追溯证据与计算内核。

设计原则
========

* 只追加（append-only）：更正产生新快照，旧值永不被覆盖或删除。
* 内容寻址：每条事件带内容哈希，任何输入或公式的变化都会改变下游节点哈希。
* 依赖显式：每次计算运行记录全部输入清单与公式版本，每个数字可回溯到输入。
* 观察与估算分离：插值/外推结果显式标注 ``estimated`` 及方法，永不冒充观测值。
* 竞争主张保留：彼此冲突的数据各自保留，等待裁定，裁定本身也是事件。
* 报告不可变：发布后冻结；撤回只追加警示标注，旧报告可按当时证据原样复现。
"""

STORE_SCHEMA_VERSION = 1
